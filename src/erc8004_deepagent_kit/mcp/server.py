from __future__ import annotations


def main() -> None:
    raise SystemExit(
        "MCP server adapter is intentionally left as a thin future integration point. "
        "Expose only the bounded ERC-8004 tools from this package when implementing MCP."
    )
